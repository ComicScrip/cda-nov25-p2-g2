name: Compile and push client and server to staging environment

on:
  push:
    branches: ["staging_deploy"]
  workflow_dispatch:

jobs:
  client-tests:
    uses: ComicScrip/cda-nov25-p2-g2/.github/workflows/test-frontend.yml@staging_deploy

  build-and-push-server:
    uses: ComicScrip/cda-nov25-p2-g2/.github/workflows/build-backend.yml@staging_deploy
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
    with:
      image_name: babyboard/cda-nov25-p2-g2-back:staging

  build-and-push-client:
    needs:
      - client-tests
    uses: ComicScrip/cda-nov25-p2-g2/.github/workflows/build-frontend.yml@staging_deploy
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
    with:
      image_name: babyboard/cda-nov25-p2-g2-front:staging